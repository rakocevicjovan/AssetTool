{% extends 'base.html.twig' %}

{% block body %}
    <h1>Game project creation</h1>

    <div class="container">
	    {{ form_start(form) }}
	        {{ form_widget(form) }}
	        <input class="btn btn-success btn-lg btn-block" type="submit" value="Create" />
	    {{ form_end(form) }}

	    <a class="btn btn-info btn-lg btn-block mt-3" href="{{ path('gameproject_index') }}">Back to the list</a>
	</div>
{% endblock %}
