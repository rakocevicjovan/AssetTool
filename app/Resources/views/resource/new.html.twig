{% extends 'base.html.twig' %}

{% block body %}
    <h1>Resource creation</h1>

    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" class="btn btn-success btn-lg btn-block" value="Create" />
        {{ form_end(form) }}

        <a class="btn btn-info btn-lg btn-block mt-3" href="{{ path('resource_index') }}">Back to the list</a>
    </div>
{% endblock %}
